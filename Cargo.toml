[workspace]
resolver = "2"
members = [
    "contracts/marketplace",
    "contracts/service_listing",
    "contracts/dispute",
]

[workspace.dependencies]
soroban-sdk = "21.0.0"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
codegen-units = 1
strip = "symbols"
